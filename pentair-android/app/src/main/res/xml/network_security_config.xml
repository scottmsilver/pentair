<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Daemon runs on the local network over HTTP (no TLS).
         Android requires explicit cleartext permission.
         We allow all hosts because the daemon IP is discovered at runtime via mDNS. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
